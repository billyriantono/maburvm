# Binaries
bin/
*.exe
*.exe~
*.dll
*.so
*.dylib
# Compiled Go binaries at repo root (build artifacts, never commit)
/panel
/create-admin
/migrate
/reset-admin-password

# Local junk / test-run artifacts
token-usage-output.txt
.pg_harness_run1/
.ignore
__pycache__/

# Vendor
vendor/

# IDE
.idea/
.vscode/
*.swp
*.swo

# Build artifacts
*.test
*.out

# Environment
.env
.env.local
# Real production env (secrets) must never be committed; keep the example tracked.
.env.production
# Allow the tracked template despite the broad .env.* exclusion below.
!.env.production.example
!.env.example

# OS
.DS_Store
Thumbs.db

# Test coverage
coverage.txt
*.cover

# Go workspace
go.work
go.work.sum

web/.next
node_modules/

# Local tooling artifacts
.commandcode/
data/
/data/

# Deploy helper — contains prod node IPs + root access, must NOT be pushed
scripts/deploy-agent.sh

# Local deploy secret files (host-side bind mounts for Compose secrets:, e.g.
# deploy/secrets/postgres_password) — must NEVER be committed.
deploy/secrets/

# Local deepwork progress
.slim/deepwork/
